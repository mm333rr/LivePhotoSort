# LivePhotoSort — Requirements
# Core script uses stdlib only; triage_sort.py uses these optional deps:
#   pip install geopy tqdm
#
# exiftool must be installed:
#   brew install exiftool

geopy
tqdm
